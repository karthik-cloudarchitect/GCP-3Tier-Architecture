# Environment Configuration for 3-Tier App

# Application Settings
APP_PORT=8080
NODE_ENV=development
ENVIRONMENT=dev

# Database Configuration
DB_HOST=localhost
DB_USER=appuser
DB_PASSWORD=your_password_here
DB_NAME=appdb
DB_CONNECTION=your-project:region:instance-name

# Security (generate strong passwords for production)
SECRET_KEY=your_secret_key_here

# Logging
LOG_LEVEL=info